# Write your MySQL query statement below
select round(sum(date_diff)/count(distinct player_id),2)
as fraction
from(
    select player_id,
    datediff(event_date,min(event_date) over (partition by player_id))=1 as date_diff
    from activity
) as new_table;

