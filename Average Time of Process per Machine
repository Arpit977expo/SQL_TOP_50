# Write your MySQL query statement below
select m1.machine_id ,round(sum(m2.timestamp-m1.timestamp)/count(distinct m1.process_id),3) as processing_time from activity m1,activity m2
where m1.activity_type='start' and m2.activity_type='end'
and m1.machine_id=m2.machine_id
and m1.process_id=m2.process_id
group by m1.machine_id;
